import SideMenu from "@/components/SideMenu";
import { Stack, Typography } from "@mui/material";

const Home = () => {
  return (
    <Stack>
      <SideMenu />
    </Stack>
  );
};

export default Home;
